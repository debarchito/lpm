/*TODO: Implement a search algorithm which \
absolute location of the plugin from server \
Also, A function to display results (Like, name and version)
*/
#![allow(unused_variables)]
use super::term;
use url::Url;

pub fn search(r#type: &str, package: &str) -> Option<Url> {
  term::pretty_print("Feature Coming Soon", 2).unwrap();
  todo!();
}
